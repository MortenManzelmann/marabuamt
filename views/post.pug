extends layout

block content
	h1= post.title
		p= post.message
		h5 Writen in #{post.ort}
		hr
		a.btn.btn-primary(href='/posts/edit/'+post._id) Edit
		a.btn.btn-danger.delete-post(href='/posts/delete/'+post._id) Delete
